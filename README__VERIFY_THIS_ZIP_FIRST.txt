MANNY/YESS WARDROBE FIX V3 - VERIFIED CONTENTS

This pack must contain these files:
- components/ProfileSwitcher.tsx
- lib/useProfiles.ts
- lib/nzTime.ts
- supabase/schema.sql

The Supabase schema seeds/renames profiles to:
- Manny
- Yess

After unzipping, from Terminal run:

  FIX_ROOT="$HOME/Downloads/wardrobe-fix-install/MANNY_YESS_FIX_V3"
  ls -la "$FIX_ROOT/components"
  ls -la "$FIX_ROOT/lib"
  grep -n "Manny\|Yess" "$FIX_ROOT/supabase/schema.sql"

If those commands do not show the files, you are not using this V3 verified zip.
